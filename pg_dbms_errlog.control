# pg_error extension, requires pg_statement_rollback
comment = 'Emulate DBMS_ERRLOG Oracle module to log DML errors in a dedicated table.'
default_version = '2.1'
module_pathname = '$libdir/pg_dbms_errlog'
relocatable = false
schema = 'dbms_errlog'
